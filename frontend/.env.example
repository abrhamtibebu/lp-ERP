# API Configuration
# For development, use the proxy path (Vite will proxy to backend)
VITE_API_BASE_URL=/api
# Backend server URL (used by Vite proxy in development)
VITE_API_TARGET=http://localhost:8000

# For production, you might want to use the full backend URL:
# VITE_API_BASE_URL=http://localhost:8000/api
# VITE_API_TARGET=http://localhost:8000

# App Configuration
VITE_APP_NAME=Leather ERP
VITE_APP_ENV=development
